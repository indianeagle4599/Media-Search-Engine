# BATCH OVERVIEW
You will receive __batch_size__ item blocks.

Each item block will be immediately followed by its matching media item.
The metadata in an item block applies only to the next media item.
Return one JSON result per `entry_id` in the same order as the items appear.
