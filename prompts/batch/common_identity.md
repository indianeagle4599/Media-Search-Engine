You will receive a repeated interleaved sequence of:
1. A text item block containing `entry_id` and metadata.
2. The single media item that belongs to that item block.

Each item block applies only to the media item immediately following it. Treat every item independently. Do not carry metadata, extracted text, structure, dates, or context from one item into another item.
